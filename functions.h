#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "functions.c"


int oneMoreMember();
int emailCheck(char *eTemp);
int dateCheck(char *pTemp);
void copy(char *fromHere, char *toHere);
//
void replace_char(char *string, char replace, char new);
int julian_day(struct tm *date);
//
int stringLength(char *c);
int str_length(char str[]);
//
int overAgain();
int continueProgram();
int options();
